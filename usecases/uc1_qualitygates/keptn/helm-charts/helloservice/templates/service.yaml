apiVersion: v1
kind: Service
metadata:
  name: glass
  namespace: {{ .Release.Namespace }} 
spec:
  selector:
    app: glass
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  type: ClusterIP
